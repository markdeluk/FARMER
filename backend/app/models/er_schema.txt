User
-	id (pk)
-	name (str)
-   surname (str)
-   role_type_id (fk, RoleType.id)
RoleType (should be an enum (consumer, farmer, producer, restaurant_owner, workshop_host, event_organizer, admin))
-   id (pk)
-   name (str)
ContactInfoType (should be an enum (telephone, email))
-   id (pk)
-   name (str)
Contact (unique(user_id, contact_info_type_id))
-   id (pk)
-   user_id (fk, User.id)
-   contact_info_type_id (fk, ContactInfoType.id)
-   contact_info (str)
Location
-   id (pk)
-   lat (float)
-   lon (float)
-   address (str)
-   zip (str)
Vendor
-   id (pk)
-   name (str)
-   description (str)
-   location_id (fk, Location.id)
-   owner_id (fk, User.id)
DayWeek (should be an enum (mon, tue, wed, thu, fri, sat, sun))
-   id (pk)
-   name (str)
OpeningHour
-   id (pk)
-   day_week_id (fk, DayWeek.id)
-   start_time (time)
-   end_time (time)
-   vendor_id (fk, Vendor.id)
Market
-	id (pk, fk, Vendor.id)
ProductCategory (should be an enum (fruit, vegetable, spice))
-   id (pk)
-   name (str)
Product
-	id (pk)
-	market_id (fk, Market.id)
-	name (str)
-   description (str, nullable)
-   category_id (fk, ProductCategory.id)
-	unit_weight (float)
-   unit_measure_id (fk, UnitMeasure.id)
UnitMeasure (should be an enum (kg, lt, g, ml))
-   id (pk)
-   name (str)
ProductDailyAvailability (unique(product_id, date))
-   id (pk)
-   product_id (fk, Product.id)
-   date (date)
-   available_quantity (int)
-   daily_price (float)
-   discounted_price (float, nullable)
-   start_time_discount (time, nullable)
-   end_time_discount (time, nullable)
ProductReservation (unique(user_id, product_id, date, time_slot))
-	id (pk)
-	user_id (fk, User.id)
-	product_id (fk, Product.id)
-	date (date)
-	time_slot (time)
-	desired_quantity (int)
Restaurant
-	id (pk, fk, Vendor.id)
RestaurantTable
-   id (pk)
-   name (str)
-   restaurant_id (fk, Restaurant.id)
RestaurantSeat
-   id (pk)
-   restaurant_table_id (fk, RestaurantTable.id)
MenuCategory (could be anything: drink, first course, antipasto, second course...)
-   id (pk)
-   name (str)
MenuItem
-   id (pk)
-   name (str)
-   price (float)
-   menu_category_id (fk, MenuCategory.id)
-   description (str)
-   restaurant_id (fk, Restaurant.id)
RestaurantBooking (unique(user_id, restaurant_seat_id, date, time_slot))
-	id (pk)
-	user_id (fk, User.id)
-	restaurant_seat_id (fk, RestaurantSeat.id)
-	date (date)
-	time_slot (time)
Rating (should be an enum (very bad, bad, average, good, very good))
-   id (pk)
-   name (str)
Review
-   id (pk)
-   rating_id (fk, Rating.id)
-   comment (str)
-   date (date)
VendorReview (unique(user_id, vendor_id))
-	id (pk, fk, Review.id)
-	user_id (fk, User.id)
-   vendor_id (fk, Vendor.id)
ProductReview (unique(user_id, product_id))
-   id (pk, fk, Review.id)
-   user_id (fk, User.id)
-   product_id (fk, Product.id)
Activity
-   id (pk, fk, Vendor.id)
-   capacity (int)
-   start_time (time)
-   end_time (time)
Workshop
-   id (pk, fk, Activity.id)
-   day_week_id (fk, DayWeek.id)
WorkshopSeat
-   id (pk)
-   workshop_id (fk, Workshop.id)
-   user_id (fk, User.id)
Event
-	id (pk, fk, Activity.id)
-   date (date)
-   organizer_fee (float)
EventSeat
-   id (pk)
-   event_id (fk, Event.id)
-   user_id (fk, User.id)
RequestStatus (should be an enum: pending, approved, rejected)
-   id (pk)
-   name (str)
WorkshopEnrollment (unique(user_id, workshop_id, date))
-	id (pk)
-	user_id (fk, User.id)
-	workshop_seat_id (fk, WorkshopSeat.id)
-	date (date)
EventEnrollment (unique(user_id, event_id))
-   id (pk)
-   user_id (fk, User.id)
-   event_seat_id (fk, EventSeat.id)
Warehouse
-   id (pk, fk, Vendor.id)
WarehouseRow
-   id (pk)
-   warehouse_id (fk, Warehouse.id)
WarehouseShelf
-   id (pk)
-   warehouse_row_id (fk, WarehouseRow.id)
WarehouseSpot
-   id (pk)
-   equipment_details (str)
-   farmer_fee (float)
-   warehouse_shelf_id (fk, WarehouseShelf.id)
StationBooking
-	id (pk)
-	user_id (fk, User.id)
-	warehouse_spot_id (fk, WarehouseSpot.id)
-   name (str)
-   description (str)
-	start_date (date)
-   end_date (date)
-	crop_type_id (fk, CropType.id)
CropType (should be an enum: basil, mint, strawberries, cherry tomatoes)
-   id (pk)
-   name (str)
RequestFlow
-   id (pk)
-   request_status_id (fk, RequestStatus.id)
-   date_time (datetime)
EventRequestFlow
-   id (pk, fk, RequestFlow.id)
-   event_id (fk, Event.id)
StationRequestFlow
-   id (pk, fk, RequestFlow.id)
-   station_booking_id (fk, StationBooking.id)